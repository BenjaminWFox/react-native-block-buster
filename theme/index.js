import View from './theme-view'
import Text from './theme-text'
import Button from './theme-button'
import styles, { colors, radius, fontFamily } from './styles'

export default {
  View, Text, styles, colors, radius, Button, fontFamily,
}
