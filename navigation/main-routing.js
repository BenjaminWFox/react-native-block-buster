import { createSwitchNavigator, createAppContainer } from 'react-navigation'
import TabsWrapped from './tabs-wrapped'

const AppContainer = createAppContainer(TabsWrapped)

export default AppContainer
